format_version: 1.3.1
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git

workflows:
  setup:
    steps:
      - npm:
          inputs:
            - command: install

  test:
    before_run:
      - setup
    steps:
      - npm:
          inputs:
            - command: test
